#!/usr/bin/env ruby

N = 100
CHUNK = 1600000

File.open("#{ENV['DATA_DIR']}/benchmark_process_xor.dat", 'w') { |f|
    N.times {
        len = CHUNK
        f.write([len].pack('V'))
        body = File.binread('/dev/zero', len)
        f.write(body)
    }
}
